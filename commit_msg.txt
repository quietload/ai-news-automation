feat: 3-stage image generation with progressive fallback

1st: Normal realistic image
2nd: If policy error → blur/silhouette version
3rd: If still fails → abstract image

This allows realistic images when possible, only applying
face blur when content policy is triggered.