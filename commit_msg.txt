feat: add keyword-based grouping for breaking news detection

- Added TOPIC_KEYWORDS for country/region-based grouping
- New functions: get_topic_key(), same_topic()
- Breaking news now groups by topic (venezuela, ukraine, etc.)
- Fixes issue where same event reported differently wasn't grouped
- Venezuela attack news now correctly detected (5 sources)